name: ci

on:
  pull_request:
  push:
    branches:
      - main
      - codex/host-rutebayar-phases

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - run: go test ./...
